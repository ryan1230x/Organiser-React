# The code below is what is needed to set up a password setup via .htaccess files
# credentials: username->ryan; password->password
# put this in the htpasswd file this is the username and the encrypted password
# ryan:$2y$10$8dif3/o2dCECNy2/4toLfOBsEir.IBRXNSHxVPJnk2YcOta29cctK
# resource to generate encrypted passwords -> https://hostingcanada.org/htpasswd-generator/

# AuthType Basic
# AuthName "Password Protected Area"
# AuthUserFile /path/to/.htpasswd
# Require valid-user